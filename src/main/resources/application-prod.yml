# 数据源
spring:
  datasource:
    # 8.0 的驱动使用cj的那个驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据库地址， useSSL: 不使用安全套接字   serverTimeZone: 时区
    url: jdbc:mysql://localhost:3306/community?serverTimezone=UTC
    username: root
    password: wwwpsxiaoji


# github OAuth 配置
github:
  client:
    id: 3fb7925ca6ea019aed64
    secret: cf43f01fac54c7c6bbd86d0d4ddb85be5c513036
  redirect:
    uri: http://zmblog.wang/community/callback